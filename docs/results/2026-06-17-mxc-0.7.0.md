# 2026-06-17 MXC SDK 0.7.0 run

Command:

```powershell
npm run all
```

Environment:

- OS: Windows 10 Enterprise 24H2, build 26100.8553
- Node: v24.16.0
- npm: 11.13.0
- Package: `@microsoft/mxc-sdk@0.7.0`
- Harness policy version: `0.7.0-alpha`

Summary:

```text
MXC harness: 13/31 passed
```

## Current failures

Python does not start in the MXC sandbox:

- `SMOKE: Python file write allowed`
- `DIAG: Python --version`
- `DIAG: Python --version with broad runtime read`
- `DIAG: Python --version with full env`

All Python failures exited with `0xC0000022` (`3221225506`). Adding a broad runtime read root such as `C:\` did not change the result.

Windows PowerShell does not start in the MXC sandbox:

- `SMOKE: PowerShell Set-Content allowed`
- `DIAG: Windows PowerShell version`
- `DIAG: Windows PowerShell version with full env`

All PowerShell startup failures exited with `0xC0000142` (`3221225794`, `STATUS_DLL_INIT_FAILED`).

PowerShell-dependent network cases are therefore reported as unsupported rather than as confirmed network-policy results:

- `NET: Invoke-WebRequest HTTPS blocked`
- `NET: direct TCP connect to 8.8.8.8:53 blocked`
- `NET: direct UDP DNS send to 8.8.8.8:53 blocked`
- `NET: .NET DNS lookup blocked`
- `NET: DnsQuery_W blocked`
- `NET: GetAddrInfoW blocked`
- `NET: .NET Ping blocked`
- `NET: IcmpSendEcho blocked`
- `NET: direct TCP connect to 1.1.1.1:445 blocked`
- `NET: hostname TCP connect to example.com:445 blocked`
- `NET: LOGON_NETCREDENTIALS_ONLY UNC blocked`

## Passing areas

Basic `cmd.exe` workspace writes still work.

Non-PowerShell negative network probes pass:

- `NET: nslookup explicit resolver blocked`
- `NET: ping.exe blocked`
- `NET: UNC dir to IP blocked`
- `NET: UNC dir to hostname blocked`
- `NET: net view UNC blocked`
- `NET: net use IPC$ blocked`

`.git` protection now passes, including protection before the `.git` directory exists:

- `GIT: missing .git directory cannot be created`
- `GIT: missing .git\config write denied`
- `GIT: .GiT case-variation write denied`
- `GIT: existing .git\config remains read-only`

## Delta from the earlier 0.2.0 run

Addressed:

- Missing `.git` path protection is fixed in the current SDK run.
- Read-only `.git\config` inside an otherwise writable workspace is fixed in the current SDK run.

Still failing:

- Python startup remains broken, including with broad runtime read access.

New or worse in this environment compared with the earlier 0.2.0 harness run:

- Windows PowerShell startup now fails with `STATUS_DLL_INIT_FAILED`.
- PowerShell-based network cases cannot be interpreted because the runtime does not start.
