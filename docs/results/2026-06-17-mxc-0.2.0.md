# 2026-06-17 MXC SDK 0.2.0 run

This run used the earlier SDK version that was pinned in the lockfile even though `package.json` declared `@microsoft/mxc-sdk` as `latest`.

Command:

```powershell
node .\src\cli.mjs --suite all
```

Environment:

- OS: Windows 10 Enterprise 24H2, build 26100.8553
- Node: v24.16.0
- npm: 11.13.0
- Package: `@microsoft/mxc-sdk@0.2.0`
- Harness policy version: `0.5.0-alpha`

Summary:

```text
MXC harness: 25/31 passed
```

The runner also hit a cleanup error after printing results:

```text
EBUSY: resource busy or locked, rmdir '...\net_unc_hostname_blocked\workspace'
```

That cleanup issue is not counted here as an MXC policy failure because the harness results had already been printed.

## Current failures

Python did not start in the MXC sandbox:

- `SMOKE: Python file write allowed`
- `DIAG: Python --version`
- `DIAG: Python --version with broad runtime read`
- `DIAG: Python --version with full env`

All Python failures exited with `0xC0000022` (`3221225506`). Adding a broad runtime read root such as `C:\` did not change the result.

`.git` protection failed when the protected path was inside a writable workspace:

- `GIT: missing .git directory cannot be created`
- `GIT: existing .git\config remains read-only`

The missing `.git` directory case created `.git` successfully. The existing `.git\config` case overwrote the protected config file.

## Passing areas

Basic `cmd.exe` workspace writes worked.

PowerShell startup and PowerShell workspace writes worked after the harness used `-ExecutionPolicy Bypass` for the script-file smoke test:

- `SMOKE: PowerShell Set-Content allowed`
- `DIAG: Windows PowerShell version`
- `DIAG: Windows PowerShell version with full env`

PowerShell-hosted API-level network probes passed as blocked:

- `NET: Invoke-WebRequest HTTPS blocked`
- `NET: direct TCP connect to 8.8.8.8:53 blocked`
- `NET: direct UDP DNS send to 8.8.8.8:53 blocked`
- `NET: .NET DNS lookup blocked`
- `NET: DnsQuery_W blocked`
- `NET: GetAddrInfoW blocked`
- `NET: .NET Ping blocked`
- `NET: IcmpSendEcho blocked`
- `NET: direct TCP connect to 1.1.1.1:445 blocked`
- `NET: hostname TCP connect to example.com:445 blocked`
- `NET: LOGON_NETCREDENTIALS_ONLY UNC blocked`

Executable-level network probes also passed as blocked:

- `NET: nslookup explicit resolver blocked`
- `NET: ping.exe blocked`
- `NET: UNC dir to IP blocked`
- `NET: UNC dir to hostname blocked`
- `NET: net view UNC blocked`
- `NET: net use IPC$ blocked`

## Delta versus the original gist run

Improved:

- PowerShell startup worked.
- Basic PowerShell workspace writes worked once the harness avoided host execution-policy interference.
- API-level network blocking became testable and passed because PowerShell probes could run.

Still failing:

- Python startup remained broken.
- Missing `.git` path protection remained broken.
- Existing `.git\config` protection inside a writable workspace remained broken.

## Delta versus the 0.7.0 run

The `0.2.0` run had better runtime compatibility in this environment:

- `0.2.0`: PowerShell started successfully, so PowerShell smoke and network probes could run.
- `0.7.0`: PowerShell failed at startup with `0xC0000142` (`STATUS_DLL_INIT_FAILED`), making PowerShell-dependent network cases unsupported.

The `0.7.0` run fixed `.git` protection:

- `0.2.0`: missing `.git` and existing `.git\config` protection failed.
- `0.7.0`: all `.git` protection cases passed.

Python startup failed in both runs.
